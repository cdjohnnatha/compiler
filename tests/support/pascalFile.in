program teste;
var
    x, h: real;
procedure limpar (y:integer);
var
    z, j:boolean;
procedure limpar2 (l:integer);
var
    z, j:boolean;

begin
    // y := 1 + 10;
    if (z and true) then z := false end;
end;
begin
    // y := 1 + 10;
    if (z and true) then z := false end;
end;
begin
    do
        x := 9.5
    while ( x < 222.2)
end.